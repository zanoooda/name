services:
  app:
    build: .
    container_name: name-python-debug
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "5678:5678"
    command: python -m debugpy --listen 0.0.0.0:5678 --wait-for-client main.py
